import { ValueFieldReferenceContext } from "../parser/asn1Parser";

/**
 * Grammar
 * ```
 * ```
 */
export class ValueFieldReferenceVisitor {
  public visitChildren(ctx: ValueFieldReferenceContext) {
  }
}
