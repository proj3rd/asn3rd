import { ComponentConstraintContext } from "../parser/asn1Parser";

/**
 * Grammar
 * ```
 * ```
 */
export class ComponentConstraintVisitor {
  public visitChildren(ctx: ComponentConstraintContext) {
  }
}
