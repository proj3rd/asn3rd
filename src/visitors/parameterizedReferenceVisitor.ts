import { ParameterizedReferenceContext } from "../parser/asn1Parser";

/**
 * Grammar
 * ```
 * ```
 */
export class ParameterizedReferenceVisitor {
  public visitChildren(ctx: ParameterizedReferenceContext) {
  }
}
