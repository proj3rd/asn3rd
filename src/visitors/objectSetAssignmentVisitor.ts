import { ObjectSetAssignmentContext } from "../parser/asn1Parser";

/**
 * Grammar
 * ```
 * ```
 */
export class ObjectSetAssignmentVisitor {
  public visitChildren(ctx: ObjectSetAssignmentContext) {
  }
}
