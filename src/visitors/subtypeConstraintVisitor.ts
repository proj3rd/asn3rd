import { SubtypeConstraintContext } from "../parser/asn1Parser";

/**
 * Grammar
 * ```
 * ```
 */
export class SubtypeConstraintVisitor {
  public visitChildren(ctx: SubtypeConstraintContext) {
  }
}
