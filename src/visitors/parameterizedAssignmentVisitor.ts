import { ParameterizedAssignmentContext } from "../parser/asn1Parser";

/**
 * Grammar
 * ```
 * ```
 */
export class ParameterizedAssignmentVisitor {
  public visitChildren(ctx: ParameterizedAssignmentContext) {
  }
}
