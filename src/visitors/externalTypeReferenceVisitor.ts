import { ExternalTypeReferenceContext } from "../parser/asn1Parser";

/**
 * Grammar
 * ```
 * ```
 */
export class ExternalTypeReferenceVisitor {
  public visitChildren(ctx: ExternalTypeReferenceContext) {
  }
}
