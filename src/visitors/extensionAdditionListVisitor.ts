import { ExtensionAdditionListContext } from "../parser/asn1Parser";

/**
 * Grammar
 * ```
 * ```
 */
export class ExtensionAdditionListVisitor {
  public visitChildren(ctx: ExtensionAdditionListContext) {
  }
}
