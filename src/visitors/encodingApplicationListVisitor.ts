import { EncodingApplicationListContext } from "../parser/asn1Parser";

/**
 * Grammar
 * ```
 * ```
 */
export class EncodingApplicationListVisitor {
  public visitChildren(ctx: EncodingApplicationListContext) {
  }
}
