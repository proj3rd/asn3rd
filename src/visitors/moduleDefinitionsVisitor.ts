import { ModuleDefinitionsContext } from "../parser/asn1Parser";

/**
 * Grammar
 * ```
 * ```
 */
export class ModuleDefinitionsVisitor {
  public visitChildren(ctx: ModuleDefinitionsContext) {
  }
}
