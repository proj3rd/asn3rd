import { ObjectClassReferenceContext } from "../parser/asn1Parser";

/**
 * Grammar
 * ```
 * ```
 */
export class ObjectClassReferenceVisitor {
  public visitChildren(ctx: ObjectClassReferenceContext) {
  }
}
