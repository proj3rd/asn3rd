import { ExtensionAndExceptionContext } from "../parser/asn1Parser";

/**
 * Grammar
 * ```
 * ```
 */
export class ExtensionAndExceptionVisitor {
  public visitChildren(ctx: ExtensionAndExceptionContext) {
  }
}
