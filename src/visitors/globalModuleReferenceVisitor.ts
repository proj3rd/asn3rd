import { GlobalModuleReferenceContext } from "../parser/asn1Parser";

/**
 * Grammar
 * ```
 * ```
 */
export class GlobalModuleReferenceVisitor {
  public visitChildren(ctx: GlobalModuleReferenceContext) {
  }
}
