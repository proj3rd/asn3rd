import { CombinedEncodingsContext } from "../parser/asn1Parser";

/**
 * Grammar
 * ```
 * ```
 */
export class CombinedEncodingsVisitor {
  public visitChildren(ctx: CombinedEncodingsContext) {
  }
}
