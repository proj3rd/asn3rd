import { EncodingObjectReferenceContext } from "../parser/asn1Parser";

/**
 * Grammar
 * ```
 * ```
 */
export class EncodingObjectReferenceVisitor {
  public visitChildren(ctx: EncodingObjectReferenceContext) {
  }
}
