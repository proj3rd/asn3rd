import { ExtensionEndMarkerContext } from "../parser/asn1Parser";

/**
 * Grammar
 * ```
 * ```
 */
export class ExtensionEndMarkerVisitor {
  public visitChildren(ctx: ExtensionEndMarkerContext) {
  }
}
