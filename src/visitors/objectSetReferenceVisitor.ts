import { ObjectSetReferenceContext } from "../parser/asn1Parser";

/**
 * Grammar
 * ```
 * ```
 */
export class ObjectSetReferenceVisitor {
  public visitChildren(ctx: ObjectSetReferenceContext) {
  }
}
