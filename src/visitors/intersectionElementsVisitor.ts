import { IntersectionElementsContext } from "../parser/asn1Parser";

/**
 * Grammar
 * ```
 * ```
 */
export class IntersectionElementsVisitor {
  public visitChildren(ctx: IntersectionElementsContext) {
  }
}
