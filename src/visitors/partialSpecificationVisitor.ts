import { PartialSpecificationContext } from "../parser/asn1Parser";

/**
 * Grammar
 * ```
 * ```
 */
export class PartialSpecificationVisitor {
  public visitChildren(ctx: PartialSpecificationContext) {
  }
}
